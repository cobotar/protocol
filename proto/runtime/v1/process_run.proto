syntax = "proto3";
package runtime.v1;

import "buf/validate/validate.proto";
import "geometry/v1/pose.proto";
import "google/protobuf/timestamp.proto";
import "runtime/v1/actor_assignment.proto";
import "validation/v1/predefined_string_rules.proto";
import "variance/v1/variant_configuration.proto";

option csharp_namespace = "Messages.Runtime.V1";
option go_package = "github.com/cobotar/protocol/messages/runtime/v1";

enum ProcessRunState {
  PROCESS_RUN_STATE_UNSPECIFIED = 0;
  PROCESS_RUN_STATE_WAITING = 1;
  PROCESS_RUN_STATE_IN_PROGRESS = 2;
  PROCESS_RUN_STATE_COMPLETED = 3;
  PROCESS_RUN_STATE_ABORTED = 4;
}

message RunParameter {
  string key = 1; // "color", "label_text", "customer_name"
  string value = 2;
}

// ProcessRun is only created when a concrete cell can currently satisfy it.
// Is is based upon a ProcessRecipe which defines what must be possible.
message ProcessRun {
  string id = 1;
  string name = 2;
  string icon = 3;
  string process_recipe_id = 4 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.(validation.v1.process_recipe_id_component) = true
  ];
  string order_id = 5;
  string station_id = 6 [
    // (buf.validate.field).required = true,
    (buf.validate.field).string.(validation.v1.station_id_component) = true
  ];
  string cell_id = 7 [
    // (buf.validate.field).required = true,
    (buf.validate.field).string.(validation.v1.cell_id_component) = true
  ];
  geometry.v1.LocalizedPose frame = 8;
  string root_sequence_run_id = 9 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.(validation.v1.sequence_run_id_component) = true
  ];
  repeated string sequence_run_ids = 10 [(buf.validate.field).repeated.items.string.(validation.v1.sequence_run_id_component) = true];
  repeated string task_run_ids = 11 [(buf.validate.field).repeated.items.string.(validation.v1.task_run_id_component) = true];
  ProcessRunState state = 12 [
    (buf.validate.field).required = true,
    (buf.validate.field).enum.defined_only = true
  ];
  google.protobuf.Timestamp initiated_at = 13;
  google.protobuf.Timestamp ended_at = 14;
  repeated ActorAssignment assignments = 15;
  variance.v1.VariantConfiguration variant_configuration = 16;
  repeated RunParameter parameters = 17;
}

message ProcessRuns {
  repeated ProcessRun items = 1;
}
