syntax = "proto3";
package plm.v1;

option csharp_namespace = "Messages.Plm.V1";
option go_package = "github.com/cobotar/protocol/messages/plm/v1";

enum LineType {
  LINE_TYPE_UNSPECIFIED = 0;
  LINE_TYPE_SUB_ASSEMBLY = 1;
  LINE_TYPE_FASTENER = 2;
  LINE_TYPE_PLATE = 3;
  LINE_TYPE_LUBRICANT = 4;
}

// TODO: allow multiple processes to make active at the same time?
message LineMessage {
  string id = 1;
  string name = 2;
  string icon = 3;
  string description = 4;
  LineType type = 5;
  // TODO: agents
  // TODO: capabilities
}

// TODO: Distinguish between 'static' configuration and runtime, e.g. such that HMD¨s (humans) can be assigned during runtime and automatically added to running tasks.
