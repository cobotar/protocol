syntax = "proto3";
package plm.v1;

import "geometry/v1/pose.proto";

option csharp_namespace = "Messages.Plm.V1";
option go_package = "github.com/cobotar/protocol/messages/plm/v1";

enum SequenceState {
  SEQUENCE_STATE_UNSPECIFIED = 0;
  SEQUENCE_STATE_MISSING_PRECONDITION = 1;
  SEQUENCE_STATE_WAITING = 2;
  SEQUENCE_STATE_IN_PROGRESS = 3;
  SEQUENCE_STATE_COMPLETED = 4;
}

message SequenceMessage {
  string id = 1;
  string name = 2;
  string icon = 3;
  string description = 4;
  int32 sequence_number = 5;
  geometry.v1.LocalizedPose frame = 6;
  string parent_id = 7;
  repeated string sequence_ids = 8;
  repeated string task_ids = 9;
  repeated string assigned_to = 10;
  SequenceState state = 11;
  int32 completed_tasks = 12;
  bool can_bulk_complete = 13;
}

// Update published when the state of a sequence have changed
message SequenceUpdatedMessage {
  string sequence_id = 1;
  repeated string assigned_to = 2;
  SequenceState state = 3;
  int32 completed_tasks = 4;
}
